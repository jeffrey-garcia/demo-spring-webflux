# Configure the default spring active profile to local
spring.profiles.active=local

# Configure the demoapp name in the earliest phase of service configuration
# Used by Eureka client registration
spring.application.name=demo-spring-webflux